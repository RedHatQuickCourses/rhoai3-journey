= Phase 3: Token Generation — Single-Model Serving
:navtitle: Phase 3 Overview
:toc: macro

**Value:** Production-ready inference. Selection → Sizing → Tuning → Automation.

== Outcome

Associates can size, tune, and deploy a single LLM (e.g. Granite) with vLLM and GitOps.

== Takeaway

* Strategy and validated model selection; VRAM math; vLLM tuning (max-model-len, gpu-memory-utilization); automated deploy script.
* **Engineering outcome:** One model serving endpoint, tuned for stability and performance.

Use the navigation menu to work through the Deploy module (Introduction, GPU Architecture, Sizing, vLLM, Model Serving, Summary).

== Checkpoint

* InferenceService (e.g. Granite) Ready; inference API responds; deployment is scripted (GitOps).
